{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci --legacy-peer-deps --workspace=manake-web && npm run build --workspace=manake-web && npx prisma migrate deploy --schema=manake-web/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npm run start --workspace=manake-web",
    "region": "us-west2",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  }
}
